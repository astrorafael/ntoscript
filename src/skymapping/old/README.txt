This folder contains files not being used for the release version of NTOScript
skymapper module. The uniform[5-8] and zigzag[5-8] are not updated with
respect to the 1-4 counterparts. The spiral pattern files contain a bug that
makes very large RA values. (inst-values and inhertiance ?).

